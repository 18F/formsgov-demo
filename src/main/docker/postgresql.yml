version: '2'
services:
  faas-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/faas/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=faas
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
